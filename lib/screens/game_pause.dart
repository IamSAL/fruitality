import 'package:flutter/material.dart';

class GamePause extends StatelessWidget {
  const GamePause({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Pause"),
    );
  }
}
