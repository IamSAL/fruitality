import 'package:flutter/material.dart';

class GameEndLoose extends StatelessWidget {
  const GameEndLoose({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("loose"),
    );
  }
}
